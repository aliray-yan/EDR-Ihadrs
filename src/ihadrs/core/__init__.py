"""
Package: core
Purpose: Core infrastructure — configuration, event bus, scheduler,
         resource management. These modules have no dependencies on
         other IHADRS packages (except constants and exceptions)
         to avoid circular imports.
"""

from __future__ import annotations